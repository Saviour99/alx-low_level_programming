#include <stdio.h>

/**
 * main - Main function
 * Return: (0)
 */

int main(void)
{
	char lower_case = 'a';

	while
		(lower_case <= 'z');
	{
		putchar(lower_case);
		lower_case += 1;
	}
	return (0);
}
